export default {
  // 设置别名
  alias: {},
  // wis默认会排除所有node_modules编译
  // 如果需要编译部分包，请在这里添加额外的需要编译的包名
  extraBabelCompileNodeModules: [],

  // 项目导出的共享资源
  exposes: {},

  plugins: [
    '<%= packages['@wisdesign/plugin-typescript'].name %>',
    <% if (config.style === 'less') { %>
    '<%= packages['@wisdesign/plugin-less'].name %>',
    <% } %>
  ],
}
